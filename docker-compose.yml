services:
  openclaw-assistant:
    build: .
    image: openclaw-ci-defect-assistant:local
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-8000}:8000"
    restart: unless-stopped
    volumes:
      - ./configs:/app/configs:ro
